# Database Configuration
# For local Docker database (default)
DB_NAME=opsfinder
DB_USER=opsuser
DB_PASSWORD=your_secure_database_password_here

# For external database (AWS RDS, Azure Database, etc.)
# Uncomment and set DB_HOST to use external database
# DB_HOST=your-external-db-host.com
# DB_PORT=5432
# When DB_HOST is set, local database container will not be deployed

# JWT Configuration
JWT_SECRET=your_super_secure_jwt_secret_key_at_least_256_bits_long_change_this_in_production
JWT_EXPIRATION=86400000

# Spring Configuration
SPRING_PROFILES_ACTIVE=prod

# Logging
LOG_LEVEL=INFO
APP_LOG_LEVEL=INFO

# Frontend Port
FRONTEND_PORT=80

# Backend API URL (for frontend to connect to backend)
# For Docker deployment, use service name: http://backend:8080
# For development, use: http://localhost:8080
# For external backend, use full URL: https://api.your-domain.com
VITE_API_BASE_URL=http://backend:8080/api

# CORS Configuration
# ALLOWED_ORIGINS - Controls which frontend domains can access the backend API
# Format: Comma-separated list of URLs (no spaces)
#
# Development (default in application.yml): http://localhost:3000,http://localhost:5173,http://localhost:8080
# Production: Set this to your actual frontend domain(s)
#
# Examples:
#   Single domain:    ALLOWED_ORIGINS=https://your-domain.com
#   Multiple domains: ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com,https://app.your-domain.com
#
# Security: Never use "*" (wildcard) in production as it allows any domain to access your API
ALLOWED_ORIGINS=https://your-domain.com
